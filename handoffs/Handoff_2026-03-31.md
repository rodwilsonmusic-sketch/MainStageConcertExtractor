# Handoff: March 31, 2026 (BackStage Architecture & Omni Multis)

## Progress Today

### Omnisphere Multis Extraction
- Completely processed and extracted 34 Omnisphere User Multis into `omnisphere_multis_extracted.json`.
- Safely captured all 8 `liveSlots` (including correct parsing of `null`/empty slots).
- Successfully attached the **`categoryFolder`** parameter to each grouping (e.g., `Pad Multis`, `BigSynth`, `Arp Multi`) so it can be transformed tomorrow once the final JSON specification is provided.

### BackStage macOS Array Architecture Discovery
- Conducted a deep dive into `/Desktop/BackStage/data/concerts/Rod_Clone_3_Test_.json` out of the user workspace to map out the application's actual UI data layer.
- **Key Architectures mapped:** 
  1. `omnisphere` / `b3` (Legacy flat)
  2. `paged` (BigSky, BlueSky)
  3. `dynamic` (Omni Pad, the newest standard with `panels`/`subPanels` and `namespace` routing strings).

### Frame Engine Specifications (Sizing Preferences)
- Extracted and validated 6 user-crafted layout examples (now saved in `/Json Examples/` folder).
- Discovered two primary UI density profiles used strictly across the app:
  * **Compact** (e.g. 120px stepped knobs, 60x40 preset buttons)
  * **Performance** (e.g. 145px stepped knobs, 56x75 preset buttons)
- Once the user finalizes the current BigSky sub-screen edit, the engine will process the diff to adopt these custom dimensions natively into the automated layout generation tool.

## Next Steps for Tomorrow
1. Inject the extracted Omnisphere multi data (`omnisphere_multis_extracted.json`) into the newly defined payload specification JSON structure.
2. Apply the final BigSky UI modifications into the `extract_bsky_upgraded.py` script to permanently lock in the specific user dimension sizes (e.g., Engine `150x105` stepped knob, `100x130` row knobs).
3. Draft the generic **Frame Descriptor Engine** template, implementing `"density": "compact"` vs `"density": "performance"` parameters.
